<?php
declare(strict_types=1); require __DIR__.'/includes/bootstrap.php'; require ROOT_PATH.'/config/database.php';
$id=(string)($_GET['id']??'');$token=(string)($_GET['token']??'');$success=false;if($id!==''&&$token!==''&&($db=database())){try{if(databaseUsesLegacySchema()){$statement=$db->prepare('UPDATE email_subscribers SET status="unsubscribed",unsubscribed_at=CURRENT_TIMESTAMP WHERE id=? AND unsubscribe_token_hash=?');$statement->execute([$id,hash('sha256',$token)]);}else{$statement=$db->prepare('UPDATE wts_subscribers SET status="unsubscribed" WHERE id=? AND token=?');$statement->execute([$id,hash('sha256',$token)]);}$success=$statement->rowCount()>0;}catch(PDOException $exception){}}
$pageTitle='Unsubscribe | Word Truth Spirit';require ROOT_PATH.'/includes/header.php';
?><main class="inner-page"><section class="page-hero"><p class="kicker">Email updates</p><h1><?= $success?'You have been unsubscribed.':'That unsubscribe link is no longer valid.' ?></h1><p><?= $success?'You will no longer receive Word Truth Spirit email updates.':'If you need help, please contact us.' ?></p><a class="button button-primary" href="<?=url('contact.php')?>">Contact us</a></section></main><?php require ROOT_PATH.'/includes/footer.php'; ?>
